package main


/** 
 * Encrypt
 * Given a file and a key, returns that file encrypted.
 * Using AES algorithm with 128 bits block size.
 */
 func encrypt(src, key []byte) []byte {

    //var newBlock cipher.Block
    //var wrong error
    
    //NewCipher(key)
    return make([]byte ,10)

 }


/** 
 * Decrypt
 * Given an encrypted file and a key, returns that file decrypted.
 * Using AES algorithm with 256 bits.
 */
 func decrypt() {
 	
 }
